/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Employee07_WorkExperienceService } from './Employee07_WorkExperience.service';

describe('Service: Employee07_WorkExperience', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Employee07_WorkExperienceService]
    });
  });

  it('should ...', inject([Employee07_WorkExperienceService], (service: Employee07_WorkExperienceService) => {
    expect(service).toBeTruthy();
  }));
});
