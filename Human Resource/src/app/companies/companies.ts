import { Employee } from "../employees/employee";

export interface Companies {
  companyId: string;
  companyName: string;
  companyPrefix: string;
  commercialId: string;
  dateStart: string;
  dateEnding: string;
  licenseId: string;
  partners: string;
  deleteFlag: number;
  
}
